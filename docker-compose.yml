services:
  web:
    image: rampraga/q360auto:${IMAGE_TAG:-latest}
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE=/app/data/q360.db
    volumes:
      - q360_data:/app/data
    restart: unless-stopped

volumes:
  q360_data:
